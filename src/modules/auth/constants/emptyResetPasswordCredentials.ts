import { IResetPasswordCredentials } from '../interfaces/resetPassword';

const emptyResetPasswordCredentials: IResetPasswordCredentials = {
    password: '',
    confirm_password: '',
};

export default emptyResetPasswordCredentials;
